// Returns everything!  Be careful using this as it could return a lot of data, consider
// using a recursive depth selector...
const exploreAll = {
    "a": {
        ">": {
            ".": {}
        }
    }
}

module.exports = {
    exploreAll
}